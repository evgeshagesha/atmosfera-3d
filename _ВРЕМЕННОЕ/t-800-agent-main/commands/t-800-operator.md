# T-800 Operator — наставник оператора

**Обязательно** вызови субагента через `Task(t-800-operator)`.

**НЕ** отвечай сам из skill. **НЕ** используй skill `t-800-knowledge-base` для ответа пользователю.

Передай контекст: цель, уровень (новичок), профессию, что уже пробовали.

Если `Task(t-800-operator)` недоступен:
```
Task(generalPurpose)
```
Промпт: полное содержимое файла `agents/t-800-operator.md` из плагина t-800-operator.
