# Tone & bans — eg-anketaplan

> 💡 Hard fail → `status: blocked_ban`. Не смягчать формулировки «чуть-чуть».

## Медицинские обещания (запрет)

Не использовать и не предлагать в UI / TG / handoff-текстах:

- «вылечим», «исцеление», «избавим навсегда»
- «секретный / революционный метод»
- «тело мечты»
- диагноз-ярлык как факт о клиенте
- гарантия медицинского результата

Допустимо: оценка состояния, безопасная нагрузка, путь настройки тела, функция / контроль — **без** physician-claim.

## Physician-claim

- Слово «врач» / «я врач» / «врачебный приём» — **никогда** в продуктовых текстах anketaplan.
- Медицинское образование в биографии (вне этого skill) — OK только в SoT-био; не тащить в copy анкеты как claim.

## Дешёвый фитнес / инфостиль

Запрещено в copy skill-предложений и UI-правки handoff:

- крик, FOMO, «успей», хэштег-шум
- anti-gym война; «без тренажёров» ≠ «без оборудования» (не путать)
- инфоцыганские гарантии и давление на оплату внутри анкеты

Тон: спокойный, премиальный, Атмосфера 3D.

## Zero-Copy цены

- Не копировать таблицы цен / пакеты в `SKILL.md`, refs, handoff YAML.
- Cite only: `20-products-prices.mdc` / `products.yaml` / vault SoT.
- Если в анкете нужен контекст продукта — **ссылка на SoT**, не paste прайса.

## PII

- Анкета содержит ПДн (имя, контакт, здоровье, привычки).
- Skill **не** автопостит PII в TG, не коммитит заполненные анкеты в git, не кладёт сырые ответы в memory fragments.
- Dev: TG только server-side после validate; LS остаётся на устройстве клиента (`egoshev_master_intake_v3`).
- В чатах HITL — минимизировать: не дублировать полные ответы клиента без нужды.

## `/anketa` ≠ `/anketaplan`

| Route | Назначение |
|-------|------------|
| `/anketa` | Существующая анкета студии — **не трогать** |
| `/anketaplan` | Месячный план после консультации (этот pack) |

Путаница routes / правка `app/anketa/**` из этого skill → **hard ban**.

## Secrets

- Не писать и не «для примера» вставлять `TELEGRAM_*`, bot token, chat id.
- Не предлагать `NEXT_PUBLIC_` для TG.
- Day-1: reuse `STRATEGY_TG_*` → `TELEGRAM_*`; не изобретать `ANKETAPLAN_TG_*`.

## CTA / домен

- Product page намеренно на `eg.egoshev.ru/anketaplan`.
- Не путать с YouTube/content правилом «не eg.egoshev.ru как CTA» для контент-каналов — здесь route = продуктовая страница.

## Sibling boundaries

- Текст программы клиенту → `eg-client-programs`, не этот skill.
- Бот P02 / Prodamus → `eg-bot-engineer`, не primary handoff anketaplan.
