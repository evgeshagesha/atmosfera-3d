# {{COMMAND_TITLE}}

**Обязательно** вызови субагента через `Task({{AGENT_NAME}})`.

Передай контекст:
- цель пользователя;
- имя/идея нового агента (если есть);
- категория плагина;
- что уже создано.

Если `Task({{AGENT_NAME}})` недоступен:

```
Task(generalPurpose)
```

Промпт: полное содержимое `agents/{{AGENT_NAME}}.md` из плагина.
