# T-800 Factory — валидация

Запусти проверки и при необходимости вызови аудитора:

```powershell
.\scripts\validate-agents.ps1
.\scripts\audit-agent-graph.ps1
```

При ошибках:

```
Task(t-800-factory-auditor)
```

Передай список проблем и пути к файлам.
