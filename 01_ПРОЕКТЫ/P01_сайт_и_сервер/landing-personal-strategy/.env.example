# Скопируйте в `.env` и заполните локально. Не коммитьте `.env`.

# Порт локального сервера (статика + /api/lead)
PORT=4177
HOST=127.0.0.1

# Telegram — только сервер. Никогда не NEXT_PUBLIC_ / не в config.js
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
